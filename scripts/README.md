# Scripts

Reserved for developer tooling (e.g. a future
`bootstrap_playwright.ps1` to run `playwright install`, or a schema
validation script wiring `schemas/*.schema.json` against
`shared/dtos`). No script is implemented in this scaffolding step to
keep this commit limited to structure, not tooling behaviour.
