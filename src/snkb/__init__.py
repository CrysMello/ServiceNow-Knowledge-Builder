"""ServiceNow Knowledge Builder.

Desktop application that observes an authenticated ServiceNow session and
produces a structured, reusable Knowledge Base (pages, elements, selectors,
navigation graph, screenshots and reports) for later consumption by the
QA ServiceNow Assistant.

This package never automates ServiceNow business actions and never
authenticates on behalf of the user.
"""

__version__ = "0.1.0"
