"""Application services: orchestrate ports to fulfill commands and
queries. Concrete implementations are added alongside each module
(AI Coding Standards, section 26); this package currently exposes only
the Application Controller contract."""
