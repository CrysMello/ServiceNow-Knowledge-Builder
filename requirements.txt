
# This file is generated for convenience in environments that expect a
# requirements.txt (e.g. simple CI steps, `pip install -r`). The
# authoritative dependency declaration is pyproject.toml — update that
# file first and mirror changes here (AI Coding Standards, DEP-002).

playwright>=1.45
customtkinter>=5.2
pydantic>=2.7
orjson>=3.10
loguru>=0.7

# --- development / test tooling ---
pytest>=8.2
pytest-cov>=5.0
pytest-asyncio>=0.23
ruff>=0.5
black>=24.4
mypy>=1.10
