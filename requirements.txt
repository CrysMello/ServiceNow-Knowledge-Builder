
# Este arquivo é gerado por conveniência para ambientes que esperam um
# requirements.txt (ex.: passos simples de CI, `pip install -r`). A
# declaração de dependências oficial é o pyproject.toml — atualize
# aquele arquivo primeiro e espelhe as mudanças aqui (AI Coding
# Standards, DEP-002).

playwright>=1.45
typer>=0.12
pydantic>=2.7
orjson>=3.10
loguru>=0.7

# --- ferramentas de desenvolvimento / teste ---
pytest>=8.2
pytest-cov>=5.0
pytest-asyncio>=0.23
ruff>=0.5
black>=24.4
mypy>=1.10
